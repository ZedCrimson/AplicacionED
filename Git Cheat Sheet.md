git init ->This help us to initialize the repository

git remote add origin https://github.com/ZedCrimson/AplicacionED.git ->This command help us to link the github repository with our local repository.

git add . + git commit ->With the git add, we select the files to commit and finally the commit is used to upload the changes or upload the files

git push origin main ->With finish to upload with this command, we upload the files to main

git checkout -b feature/update ->We create the branch named feature/update

git add . + git commit ->With the git add, we select the files to commit and finally the commit is used to upload the changes or upload the files

git checkout main ->With this command we change our actual branch to main

git merge feature/update ->We fuse the two branches

git status ->We see what files isnt commited or uploaded.
